# Replace with the token provided by BotFather. Never commit .env.
BOT_TOKEN=replace_with_your_bot_token
# Optional; relative paths are resolved from the working directory.
DATABASE_URL=sqlite+aiosqlite:///handover.sqlite3
